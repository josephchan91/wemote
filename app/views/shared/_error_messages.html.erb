<% if record.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(record.errors.count, "error") %><br>
      <% record.errors.full_messages.each do |msg| %>
        * <%= msg %><br>
      <% end %><br>
    </div>
  </div>
<% end %>
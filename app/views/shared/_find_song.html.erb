<%= form_tag searches_path, method: 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search], placeholder: "Find a song" %>
    <%= hidden_field_tag :playlist_id, playlist_id %>
    <%= submit_tag "Search", name: nil, class: "tiny button"%>
  </p>
<% end %>